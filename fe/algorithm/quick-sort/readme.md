# Quick Sort 快排

快排和归并排序非常相似，都是采用分治+递归的思路来解决问题，但他们的区别也是非常大的。

```js
递推公式：
quickSort(x, y) = quickSort(x, x + (y - x) / 2) + quickSort(x + (y - x) / 2 + 1, y)

终止条件：
x >= y
```

归并排序的思想是下层调用栈首先对均分的各个分段做好排序，然后交由上层调用栈进行合并，取分治合并结果。

快速排序的思想是通过 partition 函数划分好各个分段，然后交由下层调用栈再对划分好的分段进行排序，取分治排序结果。
